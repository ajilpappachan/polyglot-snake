#ifndef UTILS_H
#define UTILS_H

#include <stdint.h>

enum class Direction : int32_t
{
    Up,
    Right,
    Down,
    Left
};

enum class Color : int32_t
{
    Green,
    White,
    Red
};

#endif // UTILS_H